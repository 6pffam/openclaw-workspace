## Session — 2026-05-31

### What happened

#### Built: Success app (localhost:3004) — executive project timeline dashboard

Full build from scratch:
- Next.js scaffold at `workspace/success/`, port 3004
- PIN-based auth + lock screen (SQLite-backed)
- Excel upload and parse (Input tab) — column header detection, type assignment
- Projects CRUD from uploaded files
- Iterations with per-row priorities (Critical/High/Medium/Low/Exclude)
- Start date / end date / due date per row per iteration
- Row dependencies within an iteration
- Gantt chart report: activity bars (start→end), diamond milestones (due date), curved bezier dependency arrows
- Date axis, priority legend, summary stats, project + iteration selector
- Print/PDF export
- Task steps definition per project + row assignment (bulk select, filter by priority)
- Task dependencies with dropdown selector
- LaunchAgent + restart.sh for reliable deploys
- active-work/success.md created for Nexus tracking

#### Fixed: SYSMON setup

- Discovered SYSMON agent existed in openclaw.json but with no cron schedule
- Created two daily cron jobs: 14:00 and 23:00 Zurich via openclaw cron
- SYSMON model: originally ollama/qwen2.5:7b — both recent runs errored
- Both runs failing: 23:00 (model too weak), 14:00 (temp file path error)
- Model updated to anthropic/claude-haiku-3-5 in both cron payloads and agent config
- SYSMON added to Nexus crew.json so it appears on Team tab

#### Fixed: Nexus deploy stability

- Identified root cause of stale server issue: pkill -f "next start" never matches Next.js process name (runs as "next-server")
- Created restart.sh for all apps — kills by port (lsof), not process name
- LaunchAgent for success app created

### Key decisions
- Success app port: 3004
- SYSMON model: haiku (cheap, reliable, ~$0.05/month)
- Restart pattern: always kill by port, never by process name
