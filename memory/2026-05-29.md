## Session — 2026-05-29

### What happened

#### Built: Shared UI design system (@openclaw/ui)

- Created `workspace/shared-ui/` as the single design source of truth for all three apps
- Extracted Nexus dark navy theme into reusable components: Card, Button, Badge, StatusDot, PageHeader, EmptyState, NavBar
- Master `globals.css` with all CSS variables
- Dossier fully ported from white/shadcn → navy @openclaw/ui
- Universe fully ported from cream/Radix → navy @openclaw/ui
- Removed: shadcn, @base-ui/react, all Radix UI packages from Dossier and Universe
- All three apps build clean and live at :3000/:3001/:3002
- Artifact saved: 2026-05-29-shared-ui-completed.md

#### Built: Mobile responsive for Dossier + Universe

- Bottom tab nav matching Nexus mobile UX
- Safe-area padding for iPhone notch/home indicator
- Responsive single-column layouts on all key pages
- 44px minimum tap targets throughout
- PWA viewport meta added to both apps
- Artifact saved: 2026-05-29-mobile-responsive-completed.md

#### Fixed: Dossier queue trigger

- Removed gateway code from `run/route.ts`
- Case JSON now written to `dossier/cases/queue/<id>-v<N>.json` for SCOUT heartbeat pickup
- `status/route.ts` auto-heal: if status is running AND results file exists → flip to ready

#### Investigated: Nexus cost tracking

- Discovered `.trajectory.jsonl` usage data is empty — real usage is in `.jsonl` session files
- SCOUT found to have 67+ sessions on claude-sonnet-4-6 (expensive, needs monitoring)
- Multi-agent cost breakdown feature being built for Nexus
