## Session — 2026-06-01

### What happened

#### Fixed: Success app — task dependency dropdown

- Bug: dependency dropdown only showed "Installation", never "Sign SoW, issue PO"
- Root cause: task card had `overflow: hidden` clipping the absolute-positioned dropdown
- Fix 1: removed overflow:hidden — didn't fully fix (stale server also serving old build)
- Fix 2: flipped dropdown to open downward (top: 100%) — still same result
- Fix 3 (final): moved dropdown to `position: fixed` with viewport coordinates calculated via getBoundingClientRect — completely escapes all parent containers. Rendered as sibling to root layout via React fragment
- Identified systemic deploy issue: pkill -f "next start -p 3004" never matched process; new servers spawned on occupied port and died silently, leaving stale server running
- Fix: created restart.sh (kills by port), updated all LaunchAgents

#### Built: Success app — Export Image button on Report tab

- Added `html-to-image` package
- Added `chartRef` to Gantt chart container
- Export Image button: captures chart at 3× pixel ratio as PNG, named after iteration
- Button appears in blue next to Print/PDF

#### Built: Success app — IBM Carbon Design Mode on Report tab

- Analysed IBM PowerPoint template: Carbon Design System, IBM Plex Sans, black #000000 bg, accent #0F62FE
- Added toggle button "◈ IBM" on Report page
- IBM mode: pure black bg, #161616 panels, zero border-radius, IBM Plex Sans font, IBM colour palette for priorities, flat bars, square markers, vertical grid lines, IBM footer rule
- Default mode unchanged
- Both modes export correctly with correct background colour

#### Fixed: Nexus — Office tab rebuilt

- Previous version used hardcoded role IDs (coder, qa, researcher) that don't exist in crew data — all rows were empty
- Rebuilt with dynamic layout: CEO (large) → Chief of Staff (hub) → all agents in wrap grid
- SYSMON now appears on floor plan
- Active: emoji + amber glow + lit screen with content lines + pulsing dot
- Idle: empty chair 🪑 + dark desk + time since last active (e.g. "2h ago")
- SSE live updates wired

#### Added: SYSMON to Nexus Team tab

- Added sysmon entry to crew.json
- API auto-populates: live status, lastActiveAt, configuredModel (haiku), costs
- agentToAgent allow list updated to include sysmon

#### Fixed: Nexus — Memory tab stalled since May 27

- Root cause: I (fam6pfbot) stopped writing daily memory/YYYY-MM-DD.md files after May 27
- Habit failure — sessions ran but notes weren't written
- Retroactively wrote 2026-05-28 through 2026-06-01 from artifacts, wiki log, and active-work files
- Memory tab now shows 7 days of logs

### Lessons
- Memory files MUST be written at end of every session. Non-negotiable.
- Deploy: always use restart.sh, always verify PID changed before declaring done
- When implementing UI with absolute-positioned dropdowns inside overflow containers: use position:fixed with getBoundingClientRect
