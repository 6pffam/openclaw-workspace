## Session — 2026-05-28

### What happened

#### Built: Dossier — background check dashboard (localhost:3002)

Full four-phase build completed in one session:

**Phase 1 — Foundation**
- Next.js 15 app scaffolded at `workspace/dossier/`, port 3002
- SQLite schema: cases, versions, categories
- REST APIs: GET/POST /api/cases, GET /api/workspace-stats
- Case dashboard UI — white, minimal, Google Finance aesthetic
- Case cards with status chips (draft/running/ready), empty state CTA
- New Case modal (name + intent)

**Phase 2 — Case Detail / Editor**
- Route: /cases/[id]
- Full category form (Intent, Company, Individual, Time Range, Topic, Keyword, Links, Exclusions)
- Add/rename/delete categories
- Version history sidebar + Save Version button (SQLite snapshots)
- Run SCOUT button: writes case JSON → Gateway /tools/invoke → sessions_send SCOUT
- 5s polling for Running → Ready status flip
- Breadcrumb nav, inline-edit case name and intent, 800ms debounce auto-save

**Phase 3 — Results Viewer**
- Route: /cases/[id]/results/[version]
- Reads dossier/results/<case-id>-v<version>.md via gray-matter
- Editorial layout: 760px centered, blue left-bar section accents
- Sections as visual blocks (Key Findings, Entities, Timeline, Sources)
- Print-friendly CSS, not-ready state polls until file appears

**Phase 4 — SCOUT + SCRIBE Wiring**
- SCOUT AGENTS.md updated to read Dossier case JSON, run Brave Search, write results
- SCRIBE AGENTS.md updated to know Dossier results format
- Gateway token read at runtime from secrets.json (not hardcoded)
- LaunchAgent registered at ~/Library/LaunchAgents/ai.openclaw.dossier.plist

### Key decisions
- Port 3002 (3000=Nexus, 3001=Universe)
- SCOUT model upgraded to claude-sonnet-4-6 for better research quality
- Trigger: Next.js → write JSON snapshot → Gateway API → sessions_send SCOUT
- Standalone app, not integrated into Nexus
